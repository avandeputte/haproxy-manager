{
  "name": "Syslog over TCP",
  "category": "Infrastructure",
  "summary": "TCP 514 to a pool of log collectors.",
  "notes": "Senders hold the connection open indefinitely, so the timeout is long and each sender stays on one collector -- otherwise a single host's logs arrive split across several. UDP syslog cannot be proxied here.",
  "fields": {
    "url": "tcp://0.0.0.0:514",
    "target": "log1=10.0.0.1:514, log2=10.0.0.2:514",
    "balance": "source",
    "persistence": "source",
    "stick_type": "ip",
    "health": "tcp",
    "health_interval": "10s",
    "timeout_server": "24h",
    "timeout_connect": "5s"
  },
  "order": 180
}
