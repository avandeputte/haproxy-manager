{
  "name": "Docker registry",
  "category": "Applications",
  "summary": "HTTP 5000 with room to push a large image.",
  "notes": "A layer upload is one long request, so the server timeout matters more than anything else here. /v2/ answers 200 when the registry is open; with authentication it answers 401, so set the expected status to 401 in that case.",
  "fields": {
    "url": "https://registry.example.com",
    "target": "10.0.0.10:5000",
    "balance": "leastconn",
    "persistence": "none",
    "health": "http",
    "health_uri": "/v2/",
    "health_status": "200",
    "health_interval": "10s",
    "timeout_server": "1h",
    "timeout_connect": "5s",
    "cert_mode": "auto",
    "http_redirect": true
  }
}
