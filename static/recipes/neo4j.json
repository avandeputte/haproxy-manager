{
  "name": "Neo4j (Bolt)",
  "category": "Databases",
  "summary": "TCP 7687 for the Bolt protocol.",
  "notes": "Bolt is a binary protocol, so this is a TCP service; the browser interface on 7474 is an ordinary HTTP one. In a cluster the driver handles routing, so point this at a single instance or a read replica pool.",
  "fields": {
    "url": "tcp://0.0.0.0:7687",
    "target": "10.0.0.1:7687",
    "balance": "roundrobin",
    "persistence": "source",
    "stick_type": "ip",
    "timeout_server": "1h",
    "timeout_connect": "5s",
    "health": "tcp",
    "health_interval": "5s"
  },
  "order": 83
}
